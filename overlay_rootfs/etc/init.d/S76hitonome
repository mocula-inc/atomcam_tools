#!/bin/sh
#
# hitonome
#

[ -f /media/mmc/mconfig ] || exit 0

case "$1" in
  start)
    # iCamera_app が /tmp/resolv.conf を上書きするため、udhcpc に RENEW を送って DNS を復元する
    kill -USR1 $(pgrep udhcpc) > /dev/null 2>&1
    sleep 1
    /scripts/hitonome.sh on &
    ;;
  stop)
    /scripts/hitonome.sh off
    ;;
  restart|reload)
    /scripts/hitonome.sh restart
    ;;
  *)
    echo "Usage: $0 {start|stop|restart}"
    exit 1
esac

exit $?
