# mocula-backend の cdk/config/qa1.yml の s3.imageBucketName と同じ値を指定する
imageBucketName: "store.qa1.apne1.mocula-dev.com"

# ファームウェア登録API(POST /api/v1/admin/firmwares)のベースURL。
# mocula-backend の cdk/config/qa1.yml の cloudfront.domainName と同じホストを指定する
apiOrigin: "https://qa1.mocula-dev.com"
